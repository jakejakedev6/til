# GitHub Setup

## Learned

- GitHub profile READMEを作成した
- TILリポジトリを作成した
- Gitのuser.nameとuser.emailを設定した
- noreplyメールを使うと個人メールを隠せる

## Commands

```powershell
git init
git add .
git commit -m "initial commit"
git remote add origin
git push